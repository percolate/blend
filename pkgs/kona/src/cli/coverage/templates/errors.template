<% if(isError(totals, 'lines')){%>${color('ERROR:', 'red')} Insufficient line coverage of ${displayCoverage(totals, 'lines')}. Required ${options.coverageThresholds.lines}%.
<% } %><% if(isError(totals, 'branches')){%>${color('ERROR:', 'red')} Insufficient branch coverage of ${displayCoverage(totals, 'branches')}. Required ${options.coverageThresholds.branches}%.
<% } %><% if(isError(totals, 'functions')){%>${color('ERROR:', 'red')} Insufficient function coverage of ${displayCoverage(totals, 'functions')}. Required ${options.coverageThresholds.functions}%.
<% } %>
